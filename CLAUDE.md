# Claude entry point

Read `BOOTSTRAP.md` when the user asks you to apply CharterMesh to a project.
Use the same repository CLI and exact plan-hash approval flow described there.

For contributing to CharterMesh itself, also follow `AGENTS.md`.
